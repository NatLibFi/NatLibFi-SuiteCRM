q<?php

  if ( !defined('sugarEntry') || !sugarEntry ) die("Not A Valid Entry Point");

  class auth_class {
    function aski_logout_method($bean, $event, $arguments) {
// Tän pitäisi välittää logout-pyyntö
    //       header('Location: https://auth-kk.lib.helsinki.fi/saml2/idp/SingleLogoutService.php');
      // exit(0);
    }
  }
?> 